-module(fuse).

-export([macro_cell/2]).

%%====================================================================
%% macro_cell
%%====================================================================

macro_cell(MacroCell, Fuse) when is_atom(Fuse) ->
    {FB, MC} = macro_cell:split(MacroCell),
    {FB, MC, Fuse}.

%%--------------------------------------------------------------------

